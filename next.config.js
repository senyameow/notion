/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img.clerk.com',
            'files.edgestore.dev'
        ]
    }
}

module.exports = nextConfig
